<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <directory>app/Console/Kernel.php</directory>
            <directory>app/Exceptions/Handler.php</directory>
            <directory>app/Providers</directory>
        </exclude>
    </source>
    <coverage includeUncoveredFiles="true">
        <report>
            <text outputFile="php://stdout" showUncoveredFiles="false" showOnlySummary="true"/>
        </report>
    </coverage>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <env name="DB_DATABASE" value="testing"/>
        <!-- S-A-01: 空にして isConfigured() を false に倒し、テストからの外部通信を止める。
             force="true" が無いと、シェルで export 済みの環境では上書きされず保護が外れる。 -->
        <env name="GOOGLE_CLIENT_ID" value="" force="true"/>
        <env name="GOOGLE_CLIENT_SECRET" value="" force="true"/>
        <env name="GOOGLE_REDIRECT_URI" value="" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <!-- S-A-03: STRIPE_SECRET は空にして isConfigured() を false に倒し、モックを付け忘れた
             テストが本物の Stripe に Checkout Session を作ってしまう事故を止める(S-A-01 の GOOGLE_* と同じ手)。
             STRIPE_WEBHOOK_SECRET だけは固定のダミーを置く —— Webhook のテストは自分で HMAC を
             組んで署名ヘッダを作るため、値が無いと署名検証のテストが書けない。 -->
        <env name="STRIPE_SECRET" value="" force="true"/>
        <env name="STRIPE_WEBHOOK_SECRET" value="whsec_dummy_for_phpunit" force="true"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
